# 2026-07-04 工具模块重构,更具扩展性

- **Commit**: `29a7864` — refactor: 工具模块重构，更具扩展性
- **分支**: `codex/tool-registry-architecture`
- **对应 vibecoding 日志**: `docs/开发日志/vibecoding日志/2026-07-04-工具系统工程化重构设计.md`

## 改动清单

- 新增 `tool_registry.py`,引入 `ToolSpec` / `ToolRegistry` 统一管理工具 schema、确认策略、默认参数注入和安全分发。
- 将原 `tools.py` 拆分为 `tools/` 包:`bash.py`、`files.py`、`search.py`、`list_dir.py`。
- `tools/__init__.py` 继续导出 `TOOLS`、`TOOL_HANDLERS`、`NEEDS_CONFIRMATION`,兼容既有导入。
- `agent.py` 改为通过 registry 获取工具 schema 和执行工具,工具异常以 tool result 返回。
- 更新 README、CLAUDE、AGENTS 中的项目定位文案,从“最简龙虾 Agent”调整为“龙虾 Agent”。
- 补充工具调用错误包装测试,并保留文件工具扩展相关测试。

## 验证

- `python -m py_compile agent.py config.py tool_registry.py tools/*.py tests/*.py`
- `.venv/bin/python -m pytest -q` —— 42 passed
